server:
  port: 8081
  
spring:
  datasource:
    url: jdbc:mysql://dbservice:3306/user_service
    username: root
    password: test
    driver-class-name: com.mysql.cj.jdbc.Driver
  jpa:
    show-sql: true
    hibernate:
      ddl-auto: update
      
  servlet:
    multipart:
      enabled: true
      max-file-size: 200MB
      file-size-threshold: 1KB